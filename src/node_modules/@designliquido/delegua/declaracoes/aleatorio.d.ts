import { VisitanteComumInterface } from '../interfaces';
import { Declaracao } from './declaracao';
import { Bloco } from './bloco';
export declare class Aleatorio extends Declaracao {
    corpo: Bloco;
    argumentos: {
        min: number;
        max: number;
    } | null;
    constructor(linha: number, hashArquivo: number, corpo: Bloco, argumentos: {
        min: number;
        max: number;
    } | null);
    aceitar(visitante: VisitanteComumInterface): Promise<any>;
}
//# sourceMappingURL=aleatorio.d.ts.map