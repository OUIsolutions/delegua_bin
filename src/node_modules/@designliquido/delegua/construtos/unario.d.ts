import { VisitanteComumInterface, SimboloInterface } from '../interfaces';
import { Construto } from './construto';
export declare class Unario<TTipoSimbolo extends string = string> implements Construto {
    linha: number;
    hashArquivo: number;
    operador: SimboloInterface<TTipoSimbolo>;
    operando: any;
    incidenciaOperador: 'ANTES' | 'DEPOIS';
    constructor(hashArquivo: number, operador: SimboloInterface<TTipoSimbolo>, operando: Construto, incidenciaOperador?: 'ANTES' | 'DEPOIS');
    aceitar(visitante: VisitanteComumInterface): Promise<any>;
}
//# sourceMappingURL=unario.d.ts.map