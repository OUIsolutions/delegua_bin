import { TradutorInterface } from '@designliquido/delegua/interfaces';
import { ImportadorInterface } from './interfaces';
import { NucleoComum } from './nucleo-comum';
export declare class NucleoTraducao extends NucleoComum {
    importador: ImportadorInterface<any, any>;
    tradutor: TradutorInterface<any>;
    funcaoDeRetorno: Function;
    funcaoDeRetornoMesmaLinha: Function;
    arquivosAbertos: {
        [identificador: string]: string;
    };
    conteudoArquivosAbertos: {
        [identificador: string]: string[];
    };
    comandoTraducao: string;
    extensoes: {
        assemblyscript: string;
        delegua: string;
        javascript: string;
        js: string;
        alg: string;
        visualg: string;
        python: string;
        py: string;
    };
    constructor(funcaoDeRetorno?: Function, funcaoDeRetornoMesmaLinha?: Function);
    iniciarTradutor(comandoTraducao: string): void;
    /**
     * Realiza a tradução do arquivo passado como parâmetro no comando de execução.
     * @param caminhoRelativoArquivo O caminho do arquivo.
     * @param gerarArquivoSaida Se o resultado da tradução deve ser escrito em arquivo.
     *                          Se verdadeiro, os arquivos de saída são escritos no mesmo diretório
     *                          do arquivo passado no primeiro parâmetro.
     */
    traduzirArquivo(caminhoRelativoArquivo: string, gerarArquivoSaida: boolean): void;
}
//# sourceMappingURL=nucleo-traducao.d.ts.map